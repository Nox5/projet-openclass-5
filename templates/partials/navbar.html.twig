<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="{{ path("main") }}">BDiscute</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-link active" href="{{ path("main") }}">Accueil <span class="sr-only">(current)</span></a>
      <a class="nav-link" href="{{ path("top5") }}">Top 5 BD du mois</a>
      {% if is_granted('ROLE_ADMIN') %}
        <a class="nav-link" href="{{ path("admin") }}">Mon compte</a>
        <a class="nav-link" href="{{ path("app_logout") }}">Deconnexion</a>
      {% elseif is_granted('IS_AUTHENTICATED_REMEMBERED') %}
        <a class="nav-link" href="{{ path("app_logout") }}">Deconnexion</a>
      {% else %}
        <a class="nav-link" href="{{ path("app_login") }}">Se connecter</a>
        <a class="nav-link" href="{{ path("app_register") }}">S'inscrire</a>
      {% endif %}
      
    </div>
  </nav>