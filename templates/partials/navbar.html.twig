<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="{{ path("chronique") }}">BDiscute</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-link active" href="{{ path("chronique") }}">Accueil <span class="sr-only">(current)</span></a>
      <a class="nav-link" href="{{ path("top5") }}">Top 5 BD du mois</a>
      <a class="nav-link" href="{{ path("user") }}">Mon compte</a>
      <a class="nav-link" href="#">S'inscrire</a>
    </div>
  </div>
</nav>